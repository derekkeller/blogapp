<p>
  <b>Name:</b>
  <%= @post.name %>
</p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>
  <b>Content:</b>
  <%= @post.content %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

<br />
<div id='comments'>
	<%= render @post.comments %>
</div>

	<%= link_to 'Add Comment', new_post_comment_path(@post, :format => :js), :remote => true, :id => 'new_comment_link' %>